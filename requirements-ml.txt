# Optional ML: NLP multitask BERT + ViT gallery + Chroma ingest
# Install: pip install -r requirements-ml.txt
# (bitsandbytes / peft kept separate for intent PEFT — see repo docs if needed)

transformers>=4.36.0
torch
torchvision
timm>=1.0.0
accelerate
sentencepiece
safetensors
numpy
chromadb>=0.4.0
