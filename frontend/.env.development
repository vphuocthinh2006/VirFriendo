# Local FastAPI (uvicorn on 8000). Connects API + WebSocket directly — no Vite WS proxy.
# Start backend: from repo root, e.g. uvicorn services.core.main:app --reload --port 8000
VITE_API_URL=http://localhost:8000
